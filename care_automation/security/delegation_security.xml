<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <record id="rule_care_delegation_own" model="ir.rule">
        <field name="name">Delegation: own (delegator or delegate)</field>
        <field name="model_id" ref="model_care_delegation"/>
        <field name="domain_force">['|', ('delegator_id', '=', user.id), ('delegate_id', '=', user.id)]</field>
        <field name="groups" eval="[(4, ref('base.group_user'))]"/>
        <field name="perm_read" eval="True"/>
        <field name="perm_write" eval="True"/>
        <field name="perm_create" eval="True"/>
        <field name="perm_unlink" eval="False"/>
    </record>

    <record id="rule_care_delegation_hr_all" model="ir.rule">
        <field name="name">Delegation: HR manager sees all</field>
        <field name="model_id" ref="model_care_delegation"/>
        <field name="domain_force">[(1, '=', 1)]</field>
        <field name="groups" eval="[(4, ref('hr.group_hr_manager'))]"/>
    </record>
</odoo>
