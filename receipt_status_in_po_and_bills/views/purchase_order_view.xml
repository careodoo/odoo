<?xml version="1.0" encoding="UTF-8" ?>
<odoo>
    <record id="purchase_order_form" model="ir.ui.view">
        <field name="name">purchase.order.form.inherit</field>
        <field name="model">purchase.order</field>
        <field name="inherit_id" ref="purchase.purchase_order_form"/>
        <field name="arch" type="xml">
            <field name="user_id" position="after">
                <field name="receipt_status"
                       />
            </field>
        </field>
    </record>
    <record id="purchase_order_kpis_tree" model="ir.ui.view">
        <field name="name">purchase.order.tree.inherit</field>
        <field name="model">purchase.order</field>
        <field name="inherit_id" ref="purchase.purchase_order_kpis_tree"/>
        <field name="arch" type="xml">
            <xpath expr="//field[@name='state']" position="after">
                <field name="receipt_status" optional="show" widget="badge"
                       decoration-muted="receipt_status == 'nothing'"
                       decoration-warning="receipt_status == 'to_receipt' or receipt_status == 'processing'"
                       decoration-info="receipt_status == 'partial'"
                       decoration-success="receipt_status == 'Receipted'"/>

            </xpath>
        </field>
    </record>


     <record id="purchase_order_view_tree" model="ir.ui.view">
        <field name="name">purchase.order.tree1.inherit</field>
        <field name="model">purchase.order</field>
        <field name="inherit_id" ref="purchase.purchase_order_view_tree"/>
        <field name="arch" type="xml">
            <xpath expr="//field[@name='state']" position="after">
                <field name="receipt_status" optional="show" widget="badge"
                       decoration-muted="receipt_status == 'nothing'"
                       decoration-warning="receipt_status == 'to_receipt' or receipt_status == 'processing'"
                       decoration-info="receipt_status == 'partial'"
                       decoration-success="receipt_status == 'Receipted'"/>

            </xpath>
        </field>
    </record>
</odoo>
