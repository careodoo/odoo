<?xml version="1.0" ?>
<odoo>
    <data>
        <record id="sms_template_mail_compose" model="wk.sms.template">
            <field name="name">SMS notification to customer When sales send mail</field>
            <field name="condition">sale_mail</field>
            <field name="auto_delete" eval="True"/>
            <field name="sms_body_html"><![CDATA[
              Dear {{object.partner_id.name}},
              Your {{object.state in ('draft', 'sent') and 'quotation' or 'Sales Order'}} {{object.name}} amounting in {{object.amount_total}} {{object.pricelist_id.currency_id.name}} is
              {{object.state in ('draft', 'sent') and 'ready and sent to your email for review.' or 'is Approved and sent to your email for review.'}}
                            ]]>
            </field>
        </record>
    </data>
</odoo>
