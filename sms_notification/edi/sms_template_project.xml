<?xml version="1.0" ?>
<odoo>
    <data>
        <record id="sms_template_project" model="wk.sms.template">
            <field name="name">SMS notification to user When assign task</field>
            <field name="condition">project</field>
            <field name="auto_delete" eval="True"/>
            <field name="sms_body_html"><![CDATA[
              Dear {{object.task_partner_id.name}},
              You are assigned to Task to do {{ object.name }} Before the date {{ object.date_deadline }}.
                            ]]>
            </field>
        </record>
        <record id="sms_template_project_cron" model="wk.sms.template">
            <field name="name">SMS remainder to user before 1 day of deadline</field>
            <field name="condition">project_cron</field>
            <field name="auto_delete" eval="True"/>
            <field name="sms_body_html"><![CDATA[
              Dear {{object.task_partner_id.name}},
              We remind you to do the Task {{ object.name }} Before the date {{ object.date_deadline }}.
                            ]]>
            </field>
        </record>
    </data>
</odoo>
